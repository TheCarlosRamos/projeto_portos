[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
startCommand = "cd app/present_tela && gunicorn -w 4 -b 0.0.0.0:$PORT --timeout 120 app:app"

[env]
PORT = "8080"
